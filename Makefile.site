get:
	earthly +get
	rsync -ia provider.new/. provider/.
	rm -rf provider.new

venv:
	cd 3rdparty/python && poetry install
	
plan:
	earthly +plan --stack=spiral

show:
	earthly +show --stack=spiral

apply:
	earthly --push +apply --stack=spiral

fmt:
	pants format ::
